{
  "name": "dpt-benchmark",
  "image": "mcr.microsoft.com/devcontainers/python:3.12-bookworm",
  "features": {
    "ghcr.io/devcontainers/features/common-utils:2": {}
  },
  "postCreateCommand": "pip install --index-url https://download.pytorch.org/whl/cpu torch && pip install -e '.[dev]' && sudo apt-get update && sudo apt-get install -y iproute2",
  "hostRequirements": {
    "cpus": 16
  },
  "customizations": {
    "vscode": {
      "extensions": ["ms-python.python"]
    }
  }
}
